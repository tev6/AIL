// Smoke test #4 — Result envelope + attempt cascade.
// INPUT:
// OUTPUT: 99

fn maybe_fail() -> Number {
    return error("first try always fails")
}

fn fallback() -> Number {
    return 99
}

entry main() {
    return attempt {
        try maybe_fail()
        try fallback()
    }
}
