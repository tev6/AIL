// Smoke test #1 — string + builtin call.
// INPUT: world
// OUTPUT: HELLO, WORLD!

fn shout(s: Text) -> Text {
    return upper("hello, " + s) + "!"
}

entry main(text: Text) {
    return shout(text)
}
